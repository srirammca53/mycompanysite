<%= form_for(@ourblog) do |f| %>
  <% if @ourblog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ourblog.errors.count, "error") %> prohibited this ourblog from being saved:</h2>

      <ul>
      <% @ourblog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
