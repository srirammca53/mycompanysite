<%= form_for(@aboutu) do |f| %>
  <% if @aboutu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aboutu.errors.count, "error") %> prohibited this aboutu from being saved:</h2>

      <ul>
      <% @aboutu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
